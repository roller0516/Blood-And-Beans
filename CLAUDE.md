# Blood & Beans 에이전트 작업 지침

이 파일은 이 저장소에서 작업하는 모든 코딩 에이전트의 **단일 지침 원본**이다. 규칙을 고칠 때는 중복본을 만들지 말고 이 파일을 고친다. 사용자 요청과 이 파일이 충돌하면 사용자 요청을 우선하되, 데이터 손실·보안·프로젝트 손상 가능성이 있으면 실행 전에 알린다.

## 절대 원칙

- 하드코딩하지 않는다. 게임 수치, 경로, 씬·태그·레이어·Input Action 이름, 네트워크 설정, 사용자에게 보이는 문구를 코드 여러 곳에 직접 박아 넣지 않는다. 값의 성격에 따라 이름 있는 상수, `[SerializeField]`, 설정 객체, ScriptableObject 또는 기존 데이터 원본을 사용한다.
- 단, 알고리즘 자체의 불변값(`0`, `1`, 빈 컬렉션 등)처럼 의미가 자명하고 변경 가능성이 없는 값까지 억지로 설정화하지 않는다. 값의 출처와 변경 주체가 명확해야 한다.
- **값의 자리는 셋뿐이다.** ① 기획서에 확정된 규칙 수치 → `BB.Rules`의 `static readonly` 표, 절 번호 주석 필수. ② 에디터에서 만져 정할 연출·타이밍 → `[SerializeField]`. ③ 여러 화면이 공유하는 표현 설정 → ScriptableObject. **게임 수치용 SO를 새로 만들지 않는다** — 현재 SO는 표현 설정 둘뿐이다: `UIThemeConfig`(UI 기본 디자인)와 `ItemVisualConfig`(아이템 3D 표시). 판단표는 `unity-architecture` 스킬에 있다.
- **기획서가 아직 정하지 않은 값은 박되 `ponytail:` 주석으로 표시한다.** 왜 지금 박았는지와 언제 어디로 옮기는지를 함께 적는다 (`// ponytail: 임시값. 기획서 14장에 시간 표가 없다`). **표시 없는 임시값은 임시가 아니라 그냥 하드코딩이다** — 다시 찾을 방법이 없어진다. 현재 40건이 있고 `ponytail-debt` 스킬로 모아 볼 수 있다.
- 추측하지 않는다. 클래스, 메서드, 패키지 버전, 씬 오브젝트, 프리팹 연결, 기획 규칙, 오류 원인을 실제 파일·검색 결과·에디터 상태·로그에서 확인한다.
- **Unity 작업은 무조건 Unity 스킬을 사용한다.** 「Unity 공식 스킬」 표에서 해당 스킬을 찾아 `SKILL.md`를 끝까지 읽고 그 절차대로 처리한다. 읽기 전에는 파일을 건드리지 않는다.
- 기억에 의존해 Unity API를 작성하지 않는다. 현재 프로젝트의 Unity 및 패키지 버전에서 API가 실제 존재하는지 설치된 소스, IDE 참조, 공식 문서 또는 컴파일 결과로 확인한다.
- 검증하지 않은 결과를 사실처럼 말하지 않는다. 컴파일, 테스트, 에디터 실행, 로그 확인 또는 구체적인 재현 절차 중 적절한 방법으로 실제 동작을 확인한다.
- 검증할 수 없는 환경이면 성공으로 간주하지 않는다. 무엇을 확인했고 무엇을 확인하지 못했는지, 남은 위험과 사용자가 재현할 절차를 명확히 보고한다.
- 테스트 통과나 오류 메시지를 지어내지 않는다. 실행한 명령과 관찰된 결과만 근거로 완료 여부를 판단한다.

## 프로젝트 기준

- 실제 Unity 프로젝트 루트는 `BloodAndBeans/`이다.
- Unity 버전은 `6000.5.9f1`이다. 임의로 업그레이드하거나 다운그레이드하지 않는다.
- 주요 기술은 URP 17.5, Input System 1.20, Netcode for GameObjects 2.13, UniTask(`com.cysharp.unitask`)다.
- 기존 폴더 구조와 주변 코드 스타일을 먼저 따른다. 새 규칙을 적용한다는 이유로 관련 없는 파일을 일괄 정리하지 않는다.
- 게임 규칙의 기준 문서는 `00__Docs/01__Design/1_기획서/BLOOD & BEAN — 게임 기획서.md`다. 구현보다 먼저 끝까지 읽는다. 읽기 지침은 `00__Docs/01__Design/AGENTS.md`에 있다. 코드와 기획이 다르면 추측으로 한쪽을 바꾸지 말고 차이를 보고한다.

## 아키텍처 — 어셈블리 셋, 의존은 한 방향

```
BB.Client  ──▶  BB.Game  ──▶  BB.Rules
(표현·입력·UI)   (권위·복제)     (순수 규칙)
```

- `BB.Rules`는 `noEngineReferences: true`다. **`UnityEngine`을 못 쓴다** — `Mathf` 대신 `System.Math`를 쓴다. 규칙이 씬 없이 EditMode에서 그대로 돌게 하려는 것이다. 여기에 `using UnityEngine`을 넣고 싶어지면 그 코드는 여기 있을 코드가 아니다.
- `BB.Game`은 어떤 UI도 알지 못한다. **역방향 참조는 존재하지 않으며 `.asmdef`가 컴파일러로 막는다.**
- 서버에서만 도는 메서드는 **이름이 `Server`로 끝난다.** 저장소에 70여 종이 있다. 클라이언트 코드가 직접 부르지 않는다 — RPC를 거친다.
- 복제는 **NGO 2.x 통합 `[Rpc]`만** 쓴다. 레거시 `[ServerRpc]`·`[ClientRpc]`는 한 건도 없고 새로 만들지 않는다.
- **`[Rpc(SendTo.Server)]`는 아무 클라이언트나 부를 수 있다.** 새로 만들 때는 `InvokePermission = RpcInvokePermission.Owner`로 막거나, `RpcParams`를 받아 **본문에서 `SenderClientId`로 검증한다.** 호출부의 검사는 클라이언트에 있으므로 신뢰 근거가 아니다.
- 싱글턴은 `GameManager`·`MatchDirector`·`UIManager` **셋으로 닫혀 있다.** 넷째를 만들지 않는다.
- **어셈블리 추가, 참조 방향 확대, 싱글턴 추가, 폴더 계층 추가는 사용자 승인 사항이다.** 컴파일 통과는 승인이 아니다.

새 클래스를 어디 둘지, 어느 복제 수단을 쓸지 판단할 때는 **`unity-architecture` 스킬을 읽는다.** 폴더별 배치 판단표와 실제 코드 사례가 거기 있다.

## 작업 순서

1. 코드를 작성·수정하기 전에 `Ponytail` 스킬이 제공되는지 확인하고, 사용할 수 있으면 해당 `SKILL.md`를 끝까지 읽은 뒤 그 절차를 반드시 적용한다.
2. `Ponytail` 스킬이 설치되지 않았거나 접근할 수 없으면 사용한 것처럼 가장하지 않는다. 코드 작성을 시작하기 전에 사용자에게 부재 사실과 필요한 설치 또는 경로 정보를 알린다.
3. **작업을 시작할 때 어떤 스킬을 쓸지 먼저 말한다.** 쓸 스킬 이름을 그대로 적고, 쓰지 않으면 「없음」이라고 적는다. 작업 도중에 스킬을 추가로 쓰게 되면 그 시점에 알린다.
4. 요청 범위와 관련 파일, 호출 경로, 씬·프리팹 연결, 네트워크 권한을 확인한다.
5. 검색, 참조 추적, 로그 또는 재현 결과 없이 원인을 단정하거나 존재하지 않는 API·패키지·오브젝트를 만들어내지 않는다.
6. 문제를 해결하는 가장 작은 변경을 한다. 요청하지 않은 리팩터링과 기능 확장은 하지 않는다.
7. 컴파일, 관련 테스트, 에디터 로그 또는 재현 절차로 검증한다. 실행하지 못한 검증은 실행한 것처럼 말하지 않는다.
8. 완료 시 변경 내용, 검증 결과, 남은 위험, **실제로 사용한 스킬**을 간결하게 보고한다. 3번에서 예고한 것과 달라졌으면 무엇이 왜 달라졌는지 적는다. 「없음」도 보고 항목이다.

## 에디터 조작 — Unity CLI

**Unity 에디터 조작은 MCP로 하지 않는다. `unity` CLI로 한다.** 공식 브리지는 `com.unity.pipeline`이고 이 프로젝트에 이미 설치돼 있다. 서드파티 `com.coplaydev.unity-mcp`는 쓰지 않는다.

- 씬·프리팹·에셋·오브젝트를 건드리기 전에 `unity status`로 붙어 있는 에디터를 확인한다. `state`가 `ready`여야 한다.
- 명령 이름을 기억으로 짐작하지 않는다. `unity command`로 그 에디터가 실제 노출하는 목록과 파라미터를 확인하고 쓴다. 노출 목록은 에디터가 정하며 버전에 따라 달라진다.
- 출력을 파싱할 때는 `--format json`을 붙인다. 실패는 stderr가 아니라 stdout의 `success: false`로 판단한다.
- **에디터가 살아 있는 동안 `.unity`·`.prefab`·`.asset` YAML을 직접 편집하지 않는다.** 리임포트 전까지 반영되지 않고, fileID·GUID를 손으로 맞추다 조용히 깨진다.
- 연결이 안 되거나 명령이 타임아웃하면 Safe Mode를 먼저 의심한다. C# 컴파일 오류가 있으면 Pipeline 패키지가 로드되지 않는다. `unity pipeline list`로 확인하고, 컴파일 오류를 고친 뒤 에디터를 재시작한다.
- 화면 확인은 `capture_game_view`·`capture_scene_view`·`screenshot`, 로그 확인은 `get_console_logs`, 테스트는 `run_tests`로 한다. 확인하지 못했으면 미검증으로 보고한다.
- 상세 절차는 `.agents/skills/unity-cli/SKILL.md`와 그 `references/`에 있다. 작업을 시작하기 전에 읽는다.

## 그 외 MCP

- `unreal` MCP는 사용하지 않는다. 이 저장소는 Unity 프로젝트다.
- 코드베이스 구조·의존 관계·변경 영향 범위를 파악할 때는 `graphify` MCP를 먼저 쓴다. 결과는 실제 파일로 한 번 더 확인한다.
- MCP가 연결되지 않았거나 호출이 실패하면 사용한 것처럼 말하지 않는다. 부재 사실과 대체로 수행한 확인 방법을 보고한다.

## Unity 공식 스킬

`Unity-Technologies/skills` 21개와 저장소 자체 스킬 `unity-architecture`가 `.agents/skills/`에 있고 `.claude/skills/`에서 링크로 잡힌다. 실체는 저장소 안이며 전역 설치가 아니다.

**Unity 작업은 무조건 Unity 스킬을 사용한다.** 아래 표에서 해당 스킬을 찾아 `SKILL.md`를 끝까지 읽고 그 절차대로 처리한다. 읽기 전에는 파일을 건드리지 않는다. 어느 줄에 걸리는지 애매하면 읽는 쪽을 택한다. 기억에 의존해 Unity API를 쓰지 않는다는 「절대 원칙」의 연장이다. 읽지 않았으면 사용한 것처럼 말하지 않는다.

표는 **이 저장소에서의 실사용 빈도순**이다. 위에서부터 읽고, 걸리는 첫 줄에서 멈추지 말고 해당하는 줄을 모두 쓴다.

### 상시 — 대부분의 작업이 여기 걸린다

| 작업 | 스킬 |
|---|---|
| C# 파일을 새로 만들거나 옮길 때, 서버 권위·복제 수단을 정할 때 — **저장소 자체 스킬** | `unity-architecture` |
| 씬·프리팹·에셋·오브젝트를 건드리는 **모든** 작업. 배선, 스크린샷, 콘솔 로그, 테스트 실행 | `unity-cli` |
| uGUI 캔버스·RectTransform·UI 프리팹 — **이 저장소의 UI는 전부 여기다** | `ui-ugui` |
| TextMeshPro 폰트 애셋, 한글/CJK 아틀라스, 텍스트 성능 | `optimize-text-mesh-pro` |

`unity-cli`의 상세 절차는 위 「에디터 조작 — Unity CLI」에 있다. 붙어 있는 에디터를 CLI로 몰고, 에디터 설치·헤드리스 빌드·CI도 같은 CLI를 쓴다.

### 상황 — 그 주제를 실제로 건드릴 때만

| 작업 | 스킬 |
|---|---|
| NavMesh·경로 탐색 | `initialize-ai-navigation` |
| URP 포스트프로세싱 (Volume, 블룸, 톤매핑) | `urp-postprocessing` |
| URP ScriptableRendererFeature 검수 | `validate-urp-render-graph-renderer-feature` |
| Shader Graph 커스텀 노드 (HLSL Custom Function) | `shader-graph-create-custom-node` |
| UPM 패키지 조회·설치 — 패키지 추가 자체는 사용자 승인 사항이다 | `unity-package-management` |
| 오디오 임포트 설정·믹서 비용 | `optimize-audio` |
| 다국어·로컬라이제이션 | `localization` |

### 거의 안 걸린다 — 이 저장소의 확정 사항 때문에

| 작업 | 스킬 | 왜 |
|---|---|---|
| UI 전반 (무엇을 쓸지 판단) | `ui` | uGUI로 확정이라 판단할 게 없다. `ui-ugui`로 바로 간다 |
| 스프라이트 슬라이싱·피벗·아틀라스 | `sprite-editor` | 3D 탑다운으로 확정이라 2D 스프라이트 작업이 없다 |
| WebGL/WebGPU 빌드 최적화 | `optimize-web` | 웹 빌드 대상이 아니다 |
| UI Toolkit / IMGUI | `ui-uitk`, `ui-imgui` | uGUI로 확정이다 |

### 스킬이 없는 영역

**순수 C# 게임 규칙과 NGO 네트워크 권위 코드에는 해당 스킬이 없다.** Unity 공식 스킬은 전부 에디터·에셋 쪽이다. 페이즈 전환, RPC 검증, 승리 조건 같은 코드는 아래 「Netcode에서 쓰지 말아야 할 방식」·「C# 코드 설계에서 쓰지 말아야 할 방식」·「SOLID 적용 원칙」을 기준으로 작성한다. 이때 「없음」이라고 보고하는 것이 맞다.

**승인 없이 쓰지 않는 스킬.** 이 저장소의 확정된 선택을 뒤집거나 새 의존성을 끌고 온다.

- `new-unity-project` — 신규 프로젝트 부트스트랩이다. 이 저장소에는 해당 사항이 없다.
- `setup-multiplayer-services` — Unity Multiplayer Services(UGS)로 끌고 간다. 이 프로젝트의 네트워크는 **Netcode for GameObjects 2.13 직접 사용**으로 확정돼 있다 (「프로젝트 기준」).
- `build-live-game`, `implement-in-app-purchases`, `levelplay-unity-integration`, `setup-vivox-voice-chat` — 백엔드·결제·광고·음성 SDK를 새로 붙인다. 패키지 추가는 사용자 승인 사항이다 (「Unity에서 쓰지 말아야 할 방식」).

**스킬 지시와 이 파일이 충돌하면 이 파일이 우선한다.** 스킬은 일반적인 Unity 관행을 담고 있고, 이 파일은 이 저장소의 결정을 담고 있다. 스킬이 시키는 대로 했더니 여기 규칙을 어기게 됐다면 그 사실을 보고한다.

스킬은 에이전트 권한으로 실행된다. 설치 도구가 `setup-multiplayer-services`와 `setup-vivox-voice-chat`을 Critical Risk, `sprite-editor`에 알림 1건을 표시했다 (2026-08-30 설치 시점, https://skills.sh/Unity-Technologies/skills).

## QA 게이트 — 작업이 끝나면 서브 에이전트로 검수한다

**QA는 별도 서브 에이전트가 한다.** 구현을 맡은 에이전트가 자기 작업을 검수하지 않는다. 한
단계의 개발이 끝나면, 그리고 기획서·아키텍처 Markdown 작업이 끝나면, 다음 개발을 시작하기
전에 QA를 실행한다.

### 실행 방식

- **QA 서브 에이전트를 여러 개** 띄우고 **역할을 나눠** 맡긴다. 한 에이전트가 전부 보면
  놓치는 것이 생기고, 어디까지 봤는지도 남지 않는다.
- 역할은 최소 다음 넷으로 나눈다. 범위가 늘면 역할을 쪼개되 겹치게 두지 않는다.

| 역할 | 보는 것 |
|---|---|
| **기획 대조** | 기준 기획서와 구현의 차이. 수치·규칙·페이즈 구조·승리 조건이 문서와 같은가 |
| **네트워크 권위** | 서버 권위, RPC 발신자 검증, 팀 격리, 복제 범위, 클라이언트가 조작할 수 있는 값 |
| **코어 루프** | 밤 → 전환 → 낮 → 다음 밤 순환, 밤 재료의 낮 재고 연결, 임대료·페널티·예보 |
| **런타임·회귀** | Unity 실제 플레이, Console 오류, 씬·프리팹 연결 누락, EditMode 테스트, 이전 결함의 재발 |

- **2인 멀티플레이를 기준 기획서를 기반으로 반드시 실제로 검증한다.** NGO Host 1 + Client 1
  (MPPM 가상 플레이어 포함) 이상으로 붙여 한 판을 돌린다. 팀 격리는 서로 다른 팀 2개로
  확인한다 — 한 팀만 띄우면 팀 간 오염 결함은 드러나지 않는다. 무엇을 확인해야 하는지는
  여기 적지 않는다. 기획서에서 읽는다.

### 보고서

- **기준 기획서를 근거로** 쓴다. "이상해 보인다"가 아니라 "기획서 몇 장 몇 절이 X라고 했는데
  구현은 Y다"로 적는다.
- 결함마다 **어느 부분이 어떻게 잘못됐는지 자세하고 꼼꼼하게** 설명한다. 최소 항목:
  심각도 · 기획서 근거(장·절) · 파일과 라인 · 재현 절차 · 기대 결과 · 실제 결과 · 영향 범위.
- 심각도순으로 정렬한다. **실행하지 못한 항목은 통과가 아니라 `미검증`으로 남긴다.**
- 통과한 항목도 "무엇을 어떻게 확인했는지"를 적는다. 근거 없는 통과는 통과가 아니다.

### 게이트

- 주 에이전트는 QA 보고서를 사용자에게 공유하고, 문서의 잘못된 전제와 완료 표시를 먼저
  고친다.
- **치명·높음 결함과 문서 불일치가 정리되기 전에는 후속 개발을 시작하지 않는다.**
- 개발이 끝나면 같은 시나리오로 QA 회귀 검증을 다시 돌린다. 컴파일 성공이나 EditMode
  테스트 통과만으로 완료를 선언하지 않는다.

## Unity에서 쓰지 말아야 할 방식

### 에셋과 프로젝트 파일

- `Library/`, `Temp/`, `Logs/`, `obj/`, `UserSettings/`와 자동 생성되는 `.csproj`, `.sln`을 직접 수정하거나 커밋하지 않는다.
- Unity 에셋의 `.meta` 파일을 삭제·재생성·누락하지 않는다. 에셋을 파일 시스템에서 이동해야 한다면 대응하는 `.meta`도 함께 이동하고 GUID 보존을 확인한다. 가능하면 Unity Editor에서 이동한다.
- 씬, 프리팹, `ProjectSettings`의 YAML을 대규모 문자열 치환으로 수정하지 않는다. 직렬화 참조가 깨질 수 있는 변경은 에디터에서 수행하고 Missing Script/Reference를 확인한다.
- 패키지, 렌더 파이프라인, 입력 체계, 네트워크 프레임워크를 사용자 승인 없이 추가·교체·업그레이드하지 않는다.
- 서드파티 패키지 원본을 직접 고치지 않는다. 래퍼, 어댑터, 상속 또는 프로젝트 소유 복사본을 우선한다.
- 같은 의미의 에셋을 `_new`, `_final`, `_v2`처럼 복제해 버전을 관리하지 않는다. 버전 이력은 Git을 사용한다.
- **같은 모양이 두 번 이상 나오면 파츠 프리팹으로 뽑는다.** 칸·카드·행·목록 항목처럼 반복되는 조각을 화면 프리팹 안에 복사해 늘어놓지 않는다.
  - 자리는 `Assets/Prefabs/UI/Parts/`이고 이름은 타입과 같은 `UI` 접두사를 쓴다 (`UIBoxLootSlot`, `UICharacterCard`).
  - 복사본으로 늘어놓으면 조각 하나를 고칠 때 N군데를 같이 고쳐야 하고, 하나를 빠뜨리면 그 칸만 다르게 보인다. 프리팹이면 한 곳만 고친다.
  - **조각 안의 배선은 조각이 가진다.** 화면이 조각의 자식들을 직접 이으면 배선 수가 부품 수 × 반복 수로 불어난다 — 부품 9개짜리 칸 5개면 45개다.
  - 개수가 런타임에 정해지는 목록은 파츠 프리팹을 `Instantiate`한다. 상한이 기획서로 고정된 것만 화면 프리팹에 미리 깔아 둔다.
- **파츠에는 자기 컴포넌트를 붙이고, 화면은 그 컴포넌트 하나만 잇는다.** 스크립트 없는 껍데기 프리팹을 만들어 놓고 화면이 그 자식들을 이으면, 파츠로 뽑은 이유가 사라진다 — 칸을 하나 더 놓을 때마다 부품 수만큼을 손으로 다시 이어야 한다.
  - 파츠 컴포넌트가 갖는 것: 자기 부품의 `[SerializeField]` 참조, 자기 색·크기 같은 표현 설정, 자기 상태를 그리는 메서드.
  - 화면이 갖는 것: 무엇을 보여 줄지(데이터·서식·아이콘 선택), 어느 파츠를 켜고 끌지, 파츠에서 올라온 입력을 어떻게 처리할지.
  - 파츠는 자기가 몇 번째인지 모른다. 누름은 화면이 넘긴 콜백(`Bind(Action)`)으로 돌려주고, 인덱스는 화면이 붙인다.
  - 파츠가 넘겨받는 인자가 계속 늘면 표현 설정이 아직 화면에 남아 있다는 신호다. 색표·문구처럼 파츠가 혼자 정할 수 있는 것은 파츠로 내린다.
  - **클릭·호버를 받으려고 `Button`이나 `EventTrigger`를 코드로 `AddComponent`하지 않는다.** 파츠에 이미 컴포넌트가 있으면 `IPointerClickHandler`·`IPointerEnterHandler`·`IPointerExitHandler`를 직접 구현한다. 눌린 모양이 필요할 때만 `Button`을 프리팹에 얹는다.

### MonoBehaviour와 생명주기

- `MonoBehaviour`나 `ScriptableObject`를 `new`로 생성하지 않으며 생성자에 초기화 로직을 넣지 않는다. Unity 생명주기나 팩토리 API를 사용한다.
- 모든 기능을 한 `Manager`, `GameController`, 거대 `MonoBehaviour`에 몰아넣지 않는다. 씬 연결 역할과 순수 게임 규칙을 분리하고 컴포지션을 우선한다.
- `Update`, `FixedUpdate`, `LateUpdate`를 기본 실행 수단으로 쓰지 않는다. 이벤트, 명시적 호출, 코루틴 또는 제한된 주기 갱신으로 충분하면 그것을 사용한다.
- 물리 이동과 `Rigidbody` 처리를 일반 `Update`에서 하지 않는다. 물리 단계가 필요한 처리는 `FixedUpdate`와 Rigidbody API를 사용한다.
- `Awake`, `OnEnable`, `Start`의 호출 순서에 암묵적으로 의존하지 않는다. 필요한 의존성은 직렬화 참조, 명시적 초기화 또는 검증 가능한 조립 지점으로 드러낸다.
- 이벤트를 구독하고 해제하지 않은 채 오브젝트를 파괴하지 않는다. 일반적으로 `OnEnable`/`OnDisable`, 네트워크 객체는 `OnNetworkSpawn`/`OnNetworkDespawn`을 쌍으로 맞춘다.
- 필수 참조 누락을 `?.`나 조용한 조기 반환으로 숨기지 않는다. 선택 참조만 허용하고, 필수 참조는 `OnValidate`, assertion 또는 명확한 오류로 빠르게 실패시킨다.

### 참조와 결합도

- 편의를 위해 전역 mutable static 상태, 새 싱글턴, 서비스 로케이터를 만들지 않는다. 수명과 소유권이 명확한 직접 참조, 인터페이스, 이벤트 또는 조립 루트를 사용한다.
- **참조는 `[SerializeField]`가 기본이고 `GetComponent`는 마지막 수단이다.** 주기 실행 밖이라 비용이 괜찮다는 것은 조회를 쓸 이유가 되지 않는다.
  - 프리팹이나 씬에서 이을 수 있으면 잇는다. Inspector에 보이면 빠진 참조가 눈으로 드러나고, 이 컴포넌트가 무엇에 기대는지 코드를 열지 않아도 읽힌다.
  - 이을 필드가 없어서 조회를 쓰게 됐다면, 그것은 필드를 만들라는 신호지 조회를 쓰라는 신호가 아니다.
  - 조회가 남는 자리: 런타임에 `Instantiate`한 오브젝트, 충돌·트리거로 넘어온 남의 오브젝트, 스폰 시점에 정해지는 네트워크 오브젝트. 이때도 `Awake`나 스폰 콜백에서 한 번 찾아 필드에 넣는다.
  - 배선이 많아 손이 간다는 이유로 조회로 돌아가지 않는다. 반복되는 조각이면 파츠 프리팹으로 뽑아 배선 수를 줄인다 (「에셋과 프로젝트 파일」).
  - 에디터가 떠 있으면 배선은 손으로 YAML을 고치지 말고 `unity` CLI로 넣는다 (「에디터 조작 — Unity CLI」).
- **탐색과 컴포넌트 조회를 주기 실행 안에 두지 않는다. 이 규칙은 예외 없이 적용한다.**
  - 금지 대상: `GameObject.Find`, `FindObjectOfType`, `FindFirstObjectByType`, `FindAnyObjectByType`, `FindObjectsByType`, `GetComponent` 계열, `GetComponentInChildren`, `GetComponentInParent`, `Camera.main`, `LayerMask.NameToLayer`.
  - 금지 위치: `Update`, `LateUpdate`, `FixedUpdate`, `OnGUI`, 매 틱 코루틴, 주기 갱신 타이머, 그리고 이들이 호출하는 모든 헬퍼. 갱신 주기를 0.1초로 늦춘 것은 예외가 아니다.
  - 대신 `Awake`, `Start`, `OnNetworkSpawn`에서 한 번 찾아 필드에 캐시하거나 `[SerializeField]`로 Inspector에서 연결한다. 레이어·태그는 정수 인덱스로 한 번 풀어 둔다.
  - 참조가 늦게 생기는 경우(로컬 플레이어 스폰 등)에는 `if (cached == null)` 안에서만 한 번 찾고 찾은 뒤에는 다시 찾지 않는다.
  - 스폰·입장·페이즈 전환처럼 **이벤트 한 번**에 도는 탐색은 허용한다. 판단 기준은 호출 횟수가 프레임 수에 비례하는가다.
- 싱글턴을 없앤다는 이유로 모든 통신을 ScriptableObject 이벤트 채널로 바꾸지 않는다. ScriptableObject는 공유 설정·불변 데이터에 우선 사용하며, 런타임 mutable 상태는 플레이 종료와 Domain Reload 설정에도 안전하게 초기화되는 경우에만 사용한다.
- 문자열 기반 `SendMessage`, `BroadcastMessage`, 이름 기반 오브젝트 탐색을 새 코드에 사용하지 않는다.
- 태그, 레이어, 씬 이름, Animator 파라미터, Input Action 이름을 문자열로 하드코딩하지 않는다. 기존 상수·해시 ID·에셋 참조를 재사용하고 없으면 한 곳을 진실의 원천으로 만든다.

### 성능과 비동기 처리

- 프로파일링 근거 없이 복잡한 최적화, 커스텀 풀, Jobs/Burst 전환을 도입하지 않는다. 먼저 Unity Profiler로 병목을 확인한다.
- 핫 패스에서 매 프레임 LINQ, 문자열 포매팅, 배열·리스트 생성, 반복적인 컴포넌트 탐색으로 GC 할당을 만들지 않는다.
- **비동기는 UniTask를 기본으로 쓴다.** 새 코드에서 `Task`·`async void`·코루틴을 비동기 수단으로 꺼내지 않는다. UniTask는 할당이 없고, 취소를 오브젝트 수명에 묶을 수 있으며, 메인 스레드 컨텍스트를 그대로 유지한다.
  - 호출자가 기다려야 하는 것은 `UniTask`(값이 있으면 `UniTask<T>`)를 반환한다.
  - `Start`·`OnEnable` 같은 Unity 생명주기 진입점은 `async UniTaskVoid`로 둔다. `async void`는 예외와 취소가 플레이어 루프로 새어 나가 어디서 터졌는지 남지 않는다.
  - 취소 토큰을 붙인다. MonoBehaviour는 `this.GetCancellationTokenOnDestroy()`가 기본이고, 파괴된 오브젝트를 건드리는 await를 여기서 끊는다.
  - 기다리지 않고 띄우는 작업은 `.Forget()`으로 의도를 드러낸다. 반환값을 그냥 버리지 않는다.
  - 외부 라이브러리가 `Task`를 주면 경계에서만 받고(`await task`), 프로젝트 API로는 UniTask로 감싸 넘긴다. 기존 `Task` 코드를 이 규칙 때문에 일괄 개종하지 않는다.
  - **동기로 끝나는 일을 await로 감싸 비동기처럼 보이게 하지 않는다.** 기다릴 것이 없으면 그냥 동기 메서드다. 프레임을 양보하는 것이 목적이면 무엇을 왜 양보하는지 주석으로 남긴다.
- 오브젝트 수명에 맞는 취소와 예외 처리를 둔다. 취소는 정상 종료지 실패가 아니다 — `OperationCanceledException`을 오류로 로그하지 않는다.
- 백그라운드 스레드에서 `GameObject`, `Transform`, `UnityEngine.Object` 등 Unity API를 접근하지 않는다. 결과만 메인 스레드로 전달한다.
- 메인 스레드에서 `.Wait()`, `.Result`, 긴 동기 I/O로 프레임을 막지 않는다.

## Netcode에서 쓰지 말아야 할 방식

- 클라이언트 입력이나 로컬 UI 값을 서버 검증 없이 권위 있는 게임 상태로 확정하지 않는다.
- 서버 전용 상태 변경에는 `IsServer`, 소유자 입력에는 `IsOwner` 등 권한 가드를 빠뜨리지 않는다.
- 위치·게이지·타이머처럼 계속 변하는 값을 매 프레임 RPC로 보내지 않는다. `NetworkVariable`, NetworkTransform, 서버 시간 또는 필요한 빈도의 스냅샷을 사용한다.
- RPC 이름과 방향, 신뢰성, 호출 권한을 모호하게 두지 않는다. NGO 2.13에서 실제 사용 가능한 API를 프로젝트 패키지 기준으로 확인한다.
- 네트워크 콜백 등록을 해제하지 않거나, 스폰 전 `NetworkObject`/`NetworkVariable`이 준비되었다고 가정하지 않는다.
- 서버와 클라이언트가 같은 게임 규칙을 각각 독립 계산해 불일치하게 만들지 않는다. 권위 주체 한 곳에서 결정하고 결과만 동기화한다.

## C# 코드 설계에서 쓰지 말아야 할 방식

- 단일 구현 하나를 위해 의미 없는 인터페이스·추상 클래스·팩토리 계층을 미리 만들지 않는다. 실제 교체 지점이나 테스트 경계가 있을 때만 추상화한다.
- 디자인 패턴을 적용하는 것 자체를 목표로 삼지 않는다. 현재 문제보다 복잡한 DI 컨테이너, 이벤트 버스, 상태 머신, 리포지토리 계층을 도입하지 않는다.
- 상속으로 코드 재사용부터 시도하지 않는다. Unity 컴포넌트 조합과 작은 순수 C# 객체를 우선하고, 명확한 is-a 관계에서만 상속한다.
- 클래스 하나가 입력, UI, 저장, 네트워크, 게임 규칙을 동시에 소유하게 하지 않는다. 변경 이유가 다른 책임은 분리한다.
- public 필드를 Inspector 노출 목적으로 사용하지 않는다. 기본은 `[SerializeField] private`이며 외부 변경이 필요한 경우 의도를 나타내는 메서드나 제한된 프로퍼티를 제공한다.
- 게임 밸런스 수치, 시간, 속도, 가격, 임계값을 메서드 내부에 하드코딩하지 않는다. 의미 있는 상수, 직렬화 설정 또는 설정 에셋으로 이름과 출처를 부여한다.
- 예외를 빈 `catch`로 삼키거나 로그만 남기고 손상된 상태로 계속 진행하지 않는다. 복구하거나 호출자에 전달하거나 명시적으로 실패한다.
- 로그는 `UnityEngine.Debug`가 아니라 `CDebug`로 남긴다. `Log`·`LogWarning`은 `[Conditional]`이라 릴리스 빌드에서 호출부째 사라지고, 인자로 넘긴 문자열 결합·보간 비용도 함께 사라진다. `LogError`는 릴리스에도 남는다 — 그래서 조건부가 아니다. 이 규칙 때문에 관련 없는 파일의 기존 `Debug` 호출을 일괄 치환하지 않는다.
- 경고를 없애기 위한 무분별한 null-forgiving, pragma 비활성화, 빈 테스트, 하드코딩된 성공 결과를 사용하지 않는다.
- 읽기 어려운 영리한 한 줄 코드, 과도한 축약어, 타입을 반복하는 헝가리안 표기법을 사용하지 않는다. 프로젝트의 기존 명명과 포맷을 우선한다.
- 주석으로 나쁜 구조를 정당화하지 않는다. 주석은 코드가 무엇을 하는지가 아니라 결정 이유, 네트워크 권한, Unity 제약을 설명한다.
- 주석과 XML 문서 주석(`///`)은 모두 한글로 쓴다. 타입명, API명, 기획서 참조 표기(`doc 5.6.2`)처럼 원문이 식별자인 것만 원문을 유지한다.
- **주석은 간략하게 적는다.** 한 줄로 끝낼 수 있으면 한 줄이고, 길어야 두세 줄이다. 사고 과정과 검토했다가 버린 대안을 늘어놓지 않는다 — 그 이력은 Git이 갖는다. 설명이 코드보다 길어지면 주석이 아니라 코드를 고칠 때다.

## 명명 규칙

### UI 타입은 접두사 `UI`를 붙인다

화면·팝업·위젯처럼 **UI를 소유하는 타입은 이름 앞에 `UI`를 붙인다.** 파일명도 타입명과 같게 맞춘다. Unity는 `MonoBehaviour`의 클래스명과 파일명이 다르면 컴포넌트를 붙이지 못한다.

- `UIBoxLootPopup`, `UIMatchHudScreen`, `UISettingsPopup`, `UIRoomListScreen`
- ~~`BoxLootPopup`~~, ~~`MatchHudScreen`~~, ~~`SettingsPopup`~~

적용 대상은 uGUI 계층을 직접 들고 있거나 `UIView`·`UIScreen`·`UIPopup`을 상속하는 타입이다. 게임 규칙과 복제 상태를 읽어 문자열·값을 만들기만 하는 프레젠터(`MatchHudPresenter`)와 순수 규칙 타입에는 붙이지 않는다 — 접두사는 "이 타입이 화면을 소유한다"는 표시이며, 붙일수록 좋은 장식이 아니다.

### UI 텍스트는 TextMeshPro를 쓴다

**새 UI 텍스트는 예외 없이 TextMeshPro다.** legacy `UnityEngine.UI.Text`를 새로 만들지 않는다.

- 필드와 참조 타입은 `TMP_Text`를 쓴다. `TextMeshProUGUI`는 실제로 붙일 컴포넌트를 지정할 때만 쓴다 — 그래야 나중에 다른 TMP 구현으로 바꿔도 참조가 그대로다.
- 정렬은 `TextAnchor`가 아니라 `TextAlignmentOptions`다.
- `Button`, `Image`, `LayoutGroup` 등 나머지 uGUI 컴포넌트는 그대로 쓴다. 바뀌는 것은 텍스트뿐이다.
- BB.Client와 BB.Editor asmdef가 `Unity.TextMeshPro`를 참조한다. 새 asmdef에서 TMP를 쓰면 참조를 추가한다.

**한글 폰트 애셋을 반드시 지정한다.** TMP 기본 폰트 애셋(LiberationSans SDF)에는 한글 글리프가 없어서, 폰트를 지정하지 않으면 UI의 한글이 통째로 사라진다. legacy `Text`는 시스템 폰트로 폴백해서 이 문제가 드러나지 않았다.

- 프로젝트 폰트는 `Assets/Art/Fonts/Pretendard-Regular.otf`(SIL OFL 1.1, 상업 사용 가능)다. 라이선스 전문을 같은 폴더의 `Pretendard-OFL.txt`로 함께 둔다 — OFL은 사본 배포 시 라이선스 동봉을 요구한다.
- TMP 폰트 애셋은 `Tools > Blood & Beans > 한글 TMP 폰트 애셋 만들기`로 만든다. 이 명령이 아틀라스를 **Dynamic**으로 굽고 TMP 기본 폰트로 등록한다. 한글은 완성형만 11,172자라 정적으로 구우면 아틀라스가 수십 MB가 되고 그러고도 빠진 글자가 남는다.
- Dynamic 아틀라스는 원본 폰트 파일이 프로젝트에 남아 있어야 동작한다. `.otf`를 지우지 않는다.
- 런타임에 텍스트를 만드는 코드는 폰트를 지정하지 않아도 된다. TMP가 기본 폰트 애셋을 쓴다. 화면별로 다른 폰트가 필요할 때만 `[SerializeField] TMP_FontAsset`을 열어 둔다.

**기존 타입 개명은 별도 커밋으로 한다.** 이 규칙이 생겼다는 이유로 관련 없는 파일을 일괄 정리하지 않는다(위 「프로젝트 기준」). 개명할 때는 `.cs`와 `.cs.meta`를 함께 옮겨 GUID를 보존한다 — GUID가 바뀌면 프리팹과 씬의 스크립트 참조가 전부 Missing Script가 된다. 가능하면 Unity Editor에서 이름을 바꾼다.

## SOLID 적용 원칙

모든 신규 코드와 구조 변경은 SOLID를 기준으로 검토한다. 단, SOLID를 명분으로 현재 요구보다 큰 추상화 계층을 미리 만들지 않는다.

- **SRP(단일 책임):** 클래스는 하나의 변경 이유만 가진다. 입력·표현·네트워크 권위·게임 규칙·저장을 한 클래스에 섞지 않는다. Unity 컴포넌트는 실제 생명주기와 소유권 단위로 나눈다.
- **OCP(개방-폐쇄):** 기존 안정 코드를 반복 수정하지 않고 새 동작을 추가할 수 있게 구성한다. 다만 두 번째 실제 변형이 생기기 전에는 Strategy·Factory·추상 계층을 만들지 않는다.
- **LSP(리스코프 치환):** 상속 타입은 기반 타입의 권한, 상태 전이, 입력·출력 계약을 깨지 않는다. 계약을 지킬 수 없으면 상속하지 않고 컴포지션을 사용한다.
- **ISP(인터페이스 분리):** 소비자가 쓰지 않는 메서드에 의존하지 않도록 역할별로 작고 명확한 인터페이스를 둔다. 구현이 하나뿐이고 교체·테스트 경계가 아니라면 인터페이스를 만들지 않는다.
- **DIP(의존성 역전):** 고수준 게임 규칙은 Unity 오브젝트, UI, Transport와 플랫폼 SDK의 구체 구현에 직접 의존하지 않는다. 순수 규칙·좁은 계약·직렬화 참조를 사용하며, DI 컨테이너나 서비스 로케이터는 도입하지 않는다.
- SOLID 준수 여부는 클래스 수가 아니라 변경 영향 범위, 테스트 가능성, 의존 방향과 계약 보존으로 판단한다.

## 변경 및 검증 가드레일

- 사용자의 기존 변경을 되돌리거나 덮어쓰지 않는다. 관련 없는 파일을 포맷하거나 정리하지 않는다.
- 파괴적 Git 명령, 대량 삭제, 강제 푸시, 브랜치 변경, 커밋은 명시적 요청 없이 하지 않는다.
- 새 기능에는 가능하면 Unity Test Framework의 EditMode 순수 로직 테스트를 우선하고, 생명주기·씬·네트워크 동작은 필요한 범위에서 PlayMode 또는 실제 호스트/클라이언트로 확인한다.
- 테스트가 통과하도록 제품 코드를 하드코딩하거나 실패 테스트를 삭제·완화하지 않는다.
- 컴파일 확인 없이 완료라고 하지 않는다. Unity를 실행할 수 없다면 최소한 수정한 C#의 참조와 API 버전을 실제 프로젝트 파일에서 정적 확인하고, 미검증 상태와 후속 검증 절차를 보고한다.
- 성능 개선을 주장할 때는 변경 전후 프로파일 또는 할당 수치가 있어야 한다.

## 참고한 공개 지침

별 수는 2026-08-24 확인 시점의 대략적인 GitHub 표시이며 품질 보증 수단이 아니다. 규칙은 아래 자료를 그대로 복사하지 않고 이 저장소에 맞게 선별했다.

- [PatrickJS/awesome-cursorrules](https://github.com/PatrickJS/awesome-cursorrules) — 약 40.7k stars. Unity C#을 포함한 에이전트 규칙 모음과 규칙 범위화 방식을 참고했다.
- [justinwasilenko/Unity-Style-Guide](https://github.com/justinwasilenko/Unity-Style-Guide) — 약 635 stars. 기존 프로젝트 스타일 우선, 에셋 구조, 직렬화 필드와 명명 원칙을 참고했다.
- [thomasjacobsen-unity/Unity-Code-Style-Guide](https://github.com/thomasjacobsen-unity/Unity-Code-Style-Guide) — 약 337 stars. Unity 전문가 기반 C# 가독성·일관성 지침을 참고했다.
- [UnityTechnologies/open-project-1](https://github.com/UnityTechnologies/open-project-1) — Unity Technologies 공식 오픈 프로젝트. 싱글턴의 강결합 문제와 이벤트 기반 분리 사례를 참고하되, 모든 통신을 ScriptableObject로 강제하지는 않았다.
- [jbarbier/CLAUDE.md](https://github.com/jbarbier/CLAUDE.md) — 약 483 stars. 검증 가능한 완료 조건과 `CLAUDE.md`/`AGENTS.md` 단일 원본 운영 방식을 참고했다.
- [zeke/agents.md](https://github.com/zeke/agents.md) — 약 152 stars. 추측 억제, 작은 변경, 검증 및 명확한 보고 원칙을 참고했다.
