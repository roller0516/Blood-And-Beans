# 기획서 ↔ 구현 갭 점검 — 2026-09-04

> 대상: `BLOOD & BEAN — 게임 기획서.md` (헤더 v2.5 / 변경이력 v2.6) ↔ `BloodAndBeans/Assets/Scripts` 전체
> 방식: **정적 대조만 했다.** Unity 실행·컴파일·플레이 검증은 하지 않았으므로 아래 「미검증」 표시가 붙은 항목은 통과가 아니다.
> 근거: 코드 주석의 `기획서 n장 n절` 참조 41건(`ponytail` 표시)과 실제 규칙 코드.

---

## 0. 한 줄 요약

구현이 기획서보다 앞서 있다. 빠진 것은 **기능**이 아니라 **문서**다 — 특히 ① 8/30 회의 결정이 한 줄도 반영되지 않았고, ② 코드가 41곳에서 "기획서에 값이 없다"고 임시값을 들고 있으며, ③ 9장·11장·12장·13장이 서로 모순된다.

---

## 1. 회의 결정이 기획서에 반영되지 않음 — 가장 큰 구멍

`9_회의록/2026_08/20260830 회의록.md`의 결정 5건이 기획서에 없다.

| # | 회의 결정 | 기획서 현재 | 구현 현재 | 조치 |
|---|---|---|---|---|
| 1 | **아트 풍을 3D 카툰·캐주얼·파티게임으로** (Ref. 폴가이즈/파티애니멀즈) | 12장 "**2D 도트 · 탑다운**, 타일셋 1세트 + 캐릭터 4방향" | 3D다 — URP 17.5, `CharacterController`, 3D 모델(`TripoModels`), `PlayerDissolve` 셰이더 | 12장 전면 개정 (major) |
| 2 | **단축키 확장** — 스킬 / 가방 버리기 / 가방 묻기 / 땅 파기(F) / WASD / 박스 UI 열고닫기 | 11장 "방향키 + **2개 키**", "캐릭터 능력은 전부 패시브이므로 **스킬 키가 없다**" | 전부 있다 (`PlayerInputRouter`, `UseSkillRpc`, `BuryRpc`, `DumpRpc`) | 11장 조작표 재작성 |
| 3 | **가방이 제거되면 귀환 지점 복귀 시 재생성** + 가방 아이콘 X 표시 | 6.8 "다음 밤 페이즈가 되면 가방은 다시 기본으로 지급" | 회의 결정 **미구현**. `PlayerInventory.OnPhaseEntered`가 밤 시작에만 재지급 | 기획서 확정 → 구현 |
| 4 | 스태미나 시스템 **미도입** 결론 | 언급 없음 | — | 14장이나 별도 "검토 후 제외" 목록에 기록 |
| 5 | 캐릭터 variation — 팀/플레이어 **색 표시**, 유령 베이스에 다른 몬스터 특징 | 9장·12장에 외형 규칙 없음 | `TeamColors` 존재. `CustomerLook`은 "CC0 인간 키트 임시" 주석 | 9장 또는 12장에 절 추가 |

> 3번은 **문서·구현 양쪽이 회의와 다르다.** 셋이 갈라져 있으므로 여기부터 정리해야 한다.

---

## 2. 기획서 내부 모순 — 코드가 이미 한쪽을 골랐다

| # | 모순 | 코드가 고른 쪽 | 조치 |
|---|---|---|---|
| 2-1 | **6.6** "박스 여는 중 대시 → 개봉 중단, **진행도 50% 유지**" ↔ **6.5.5** "다시 열려면 로딩부터 **처음부터**" | 6.5.5. `PlayerInteract.InterruptServer` 주석에 "진행도를 절반 남기지 않는다"고 명시 | 6.6 표의 해당 행 삭제 |
| 2-2 | **9.1 제목**은 "낮 패시브 1 + **밤 액티브** 1"인데, 본문에 밤 패시브 표(취소선)와 "밤이 강하면 낮은 무난하게" 문장이 남아 있고, **11장**은 "전부 패시브라 스킬 키가 없다"고 한다 | 밤 = 액티브. `NightSkill` enum + 쿨타임 표 구현 | 9.1 잔여 문장 정리 + 11장 동기화 |
| 2-3 | 9.1 낮 패시브 **8종** vs 9.2 밤 액티브 **5종** | `CharacterCatalog`가 캐릭터 8종 중 **3종의 밤 스킬을 "미정"으로 비워 둠** (붙임성·강심장·제빵사) | 밤 액티브 3종 추가 또는 캐릭터 수 확정 (14장 #10) |
| 2-4 | **13장 첫 검증 빌드 「빼는 것」**: 캐릭터 패시브 · 밤 액티브 · 설비 업그레이드 · 손님 종족 기믹 · 블러드 빈 | **전부 이미 구현돼 있다** | 13장을 현재 빌드 기준으로 갱신 |
| 2-5 | 13장 "귀환과 **50% 소실**" ↔ 6.8이 3갈래(전량/일부 n%/전량 소실)로 바뀜 | 6.8 3갈래. `ReturnOutcome` enum | 13장 문구 수정 |
| 2-6 | 헤더 **v2.5** ↔ 변경 이력 마지막 행 **v2.6** | — | 헤더 갱신 |
| 2-7 | **15장 버전 규칙**: "파일명 `v{major}.{minor}`, 덮어쓰지 말고 새 파일" | 저장소 사본은 `BLOOD & BEAN — 게임 기획서.md` 단일 파일을 덮어쓰는 중 | 규칙을 고치거나 운영을 고치거나 — 지금은 둘이 다르다 |
| 2-8 | **5.6.2 최종 판매가 계산식**에 **손님 종족 배수가 빠져 있다** | `SaleRegister`가 5.6.2 계산 뒤에 `Customer.PriceWeightOf`(좀비 0.7 / 뱀파이어 1.4 / 마녀 1.8)를 한 번 더 곱한다 | 5.6.2 공식에 종족 배수 항 추가 (아래 3번 표와 연결) |

---

## 3. 기획서에 값이 없어 코드가 임시값을 들고 있는 곳 (`ponytail` 41건)

CLAUDE.md 규칙대로 코드가 전부 표시해 뒀다. **★은 14장 「미결 사항」에조차 항목이 없는 것** — 즉 빠진 줄도 모르고 있는 값이다.

### 3-1. 경제 — 가장 시급

| 값 | 현재 임시값 | 기획서 | 파일 |
|---|---|---|---|
| ★ **메뉴 10종 기본가** | 30~70 | 가격표 자체가 없다. "아메리카노는 임대료를 못 따라간다"는 제약뿐 | `Rules/MenuDef.cs` |
| ★ **손님 종족 가격 가중치** | 좀비 0.7 / 뱀파이어 1.4 / 마녀 1.8 | 5.5는 "낮다/높다/가장 비싸다"로만. 5.6.2 공식에 항이 없다 | `Day/Customer.cs` |
| ★ **종족별 인내심** | 좀비 90s / 뱀파이어 30s / 나머지 60s | 상대적 순서만 확정 | `Day/Customer.cs` |
| ★ **하루 주문 수 / 손님 스폰 간격 / 동시 대기 상한** | 8건 / 8초 / 4명 | 없음 | `Economy/TransitionLedger.cs`, `Day/CustomerQueue.cs` |
| ★ **종족 등장 분포** | 균등(좀비·뱀파이어만 2배) | 없음 | `Rules/Forecast.cs` |
| 블러드 빈 판매가 배수 | 1.5 | 14장 #12 미결 | `Rules/SalePrice.cs` |
| 인기 재료가 블러드빈·업그레이드 재료에도 붙는가 | 현재 리젠 풀(흔한 6종)에서만 뽑아 **사실상 제외** | 14장 #15 미결 | `Rules/Forecast.cs` |
| ★ **업그레이드 9종의 재료 비용** | 1~3개 (UI 목업 7번에서 그대로 가져옴) | 8장에 **비용 개념 자체가 없다** | `Rules/UpgradeCatalog.cs` |
| ★ 일차별 리젠 테이블 | 3등급 희귀 칸 수 `1,1,2,2,3,3,3` | 10장이 "후반에 비중이 오른다" 방향만 | `Rules/RegenTable.cs` |
| ★ 상자 등급 등장 비율 | 자리마다 `tierWeights` | 6.3은 링 배치만 | `Night/ItemBox.cs` |

### 3-2. 낮

| 값 | 현재 | 기획서 |
|---|---|---|
| ★ 커피 조리 4초 / 오븐 9초 | `Reset()` 기본값 | 14장에 시간 표 없음 |
| 그릇 4개 / 세척 1.5초 | | 14장 #5 미결 |
| ★ 식기세척기 6초 / 자동 서빙 1초 / 전달 벨트 1.2초 | | 8장이 "느리게"라고만 |
| ★ Perfect 시 인내심 회복량 | **최대치의 25%** | 5.2는 "회복"만 |
| 탄 것 판매 시 인내심 감소 | 10 | 14장 #6 미결 |
| ★ 임대료 페널티 수치 | 제작 ×1.1 / 시야 ×0.7 / 개봉 ×1.5 | 3.3이 "10% 감소", 나머지는 "감소"라고만 |
| ★ 8일차 이후 임대료 / 4회 이상 연속 미납 | 마지막 값 유지 / Tier3 유지 | 표가 7일·3회에서 끊긴다 |

### 3-3. 밤

| 값 | 현재 | 기획서 |
|---|---|---|
| 재료별 무게 8종 / 가방 용량 | 0.3~3.0 / 20 | 14장 #3 미결 |
| ★ **귀환 실패 소실 비율 n%** | **50%** | 6.8이 문자 그대로 "n%"로 비워 둠 |
| 대시 쿨 6s / 넉백 3 / 사거리 1.6 / 거리 3.5 / 0.18s | | 14장 #7 (쿨 5~8초만 명시) |
| 안개 반경 7 / 셀 1m / 격자 240×240 | | 14장 #2 미결 |
| ★ **밤 액티브 수치 전부** — 메아리 반경 20, 감별 8, 추적 18, 추적 노출 6초 | | 9.2에 쿨타임만 있고 범위·지속이 없다 |
| ★ **묻은 가방** — 회수 0.6초 / 소각 3초 / 캔슬 이동거리 0.35m / 사거리 2 | | 6.7 「묻기」에 수치가 하나도 없다 |
| ★ 도깨비불·환각의 가짜 오브젝트 | 내용 없는 빈 상자 / 빈 가방, 개수·수명 제한 없음 | 9.2에 규정 없음 |

---

## 4. 기획서에 없는데 구현·운영에 필요한 절 (신설 필요)

| # | 없는 것 | 근거 |
|---|---|---|
| 4-1 | **로비·방·매치메이킹** | 10장 "인원 수는 로비에서 정한다" 한 줄뿐. 실제로는 `SteamLobby`(27KB) + 방 목록·방·준비·캐릭터 선택 화면이 구현돼 있다 |
| 4-2 | **캐릭터 선택 규칙** | 픽 확정 시점, 팀 내 중복 금지 판정 주체(서버), 미선택 처리, 선택 취소가 9.1 한 줄로만 존재 |
| 4-3 | **전환 화면 구성** | 4장은 "정산·업그레이드·예보"라고만. 실제 화면은 매출/청구액/납부액/부채/연속 미납 + 업그레이드 설치 카드 |
| 4-4 | **업그레이드 구매 규칙** | 비용·1회 설치·재고 차감·전환 10초 안에만 가능 — 8장에 전부 없음 |
| 4-5 | **판 종료·최종 결과 화면** | 3.1은 승리 조건만. `FinalStandings`·결과 팝업 있음. "한 판 더"는 재시작 경로가 없어 잠겨 있다 |
| 4-6 | **정보 공개 범위(팀 격리)** | 3.1 "비공개" 한 줄을 구현이 크게 확장했다 — 상대 팀 카페·손님·손에 든 아이템을 **복제 자체를 하지 않는다**. 매출만 공개. 규칙으로 명문화 필요 |
| 4-7 | **귀환 결과 표시** | 6.8에 UI 규정 없음. 구현은 유지/소실 개수와 n%를 팝업으로 준다 |
| 4-8 | **개봉 세션 취소 조건** | 6.5.5는 "이동 키"만. 구현은 이동거리 0.35m 초과·사거리 이탈·피격·페이즈 전환 |
| 4-9 | **임시 상자 사양** | 간격·접지·수명(밤 종료 시 소멸)·5종 초과 시 분할이 코드에만 있다 |
| 4-10 | **사운드** | `03__Sound`에 BGM 24곡이 있는데 기획서에 사운드 장이 없다 |

---

## 5. 기획서에 있는데 구현이 없는 것

| # | 항목 | 상태 |
|---|---|---|
| 5-1 | **미니맵** — 6.2②("걷힌 안개는 발자국이다"), 6.5.3("미니맵에 열린 박스와 잔여 내용 표시") | **미구현.** 두 절이 미니맵을 전제로 쓰였는데 미니맵이 없고, 미니맵 사양도 문서에 없다 |
| 5-2 | 가방 제거 후 귀환 지점 재생성 (8/30 회의) | 미구현 (1-3과 동일) |
| 5-3 | 3등급 상자의 안개 투과 발광 (6.5.2) | **미검증** — 셰이더·프리팹까지 확인하지 않았다 |
| 5-4 | 손님 인내심이 "캐릭터 패시브로 **회복**"된다 (5.5) | 구현은 「인기 카페」=최대치 증가, 「붙임성」=감소 정지다. **회복은 Perfect 판정만** 한다 → 문서 표현 정정 필요 |

**구현 확인된 것** (문서와 일치): 밤→낮→전환 순환, 안개 전원 공유 + 밤마다 초기화, 상자 3등급·순차 공개·공개 상태 공유, 무게 7밴드 표(6.7과 값 일치), 대시 70% 제한 / 80% 낙하, Night 0 및 밤 첫 15초 견제 불가, 임대료 표·부채 이월·페널티 3단계, 예보 70/30 · 아메리카노 20% 상한 · 인기 재료 +30% 중첩, 늑대인간 2~3개 주문, 마녀 3재료, 유령/해골 온도 태그, 묻기·소각·추적·감별·메아리·도깨비불·환각, 팀 수에 따른 상자 솎기.

---

## 6. 권장 처리 순서

1. **8/30 회의 결정 반영** → 12장(아트), 11장(조작), 6.8(가방 재생성). 아트 풍 변경은 major이므로 **v3.0**.
2. **모순 제거** — 2-1 · 2-2 · 2-4 · 2-5 · 2-8.
3. **★ 표시 값에 14장 항목 번호 부여** — 지금은 "미결로 관리되지 않는 미결"이라 가장 위험하다. 특히 메뉴 가격표·종족 가중치·업그레이드 비용은 밸런스의 뼈대다.
4. **신설 절 작성** — 4-1(로비), 4-4(업그레이드 구매), 4-6(정보 공개 범위). 이 셋은 이미 구현이 규칙을 만들어 버린 영역이다.
5. 미니맵 결정 — 넣을 것인지, 6.2②·6.5.3을 고칠 것인지.
