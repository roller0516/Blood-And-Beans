# Blood & Beans 에이전트 작업 지침

이 파일은 이 저장소에서 작업하는 모든 코딩 에이전트의 공통 계약이다. 사용자 요청과 이 파일이 충돌하면 사용자 요청을 우선하되, 데이터 손실·보안·프로젝트 손상 가능성이 있으면 실행 전에 알린다.

## 절대 원칙

- 하드코딩하지 않는다. 게임 수치, 경로, 씬·태그·레이어·Input Action 이름, 네트워크 설정, 사용자에게 보이는 문구를 코드 여러 곳에 직접 박아 넣지 않는다. 값의 성격에 따라 이름 있는 상수, `[SerializeField]`, 설정 객체, ScriptableObject 또는 기존 데이터 원본을 사용한다.
- 단, 알고리즘 자체의 불변값(`0`, `1`, 빈 컬렉션 등)처럼 의미가 자명하고 변경 가능성이 없는 값까지 억지로 설정화하지 않는다. 값의 출처와 변경 주체가 명확해야 한다.
- 추측하지 않는다. 클래스, 메서드, 패키지 버전, 씬 오브젝트, 프리팹 연결, 기획 규칙, 오류 원인을 실제 파일·검색 결과·에디터 상태·로그에서 확인한다.
- 기억에 의존해 Unity API를 작성하지 않는다. 현재 프로젝트의 Unity 및 패키지 버전에서 API가 실제 존재하는지 설치된 소스, IDE 참조, 공식 문서 또는 컴파일 결과로 확인한다.
- 검증하지 않은 결과를 사실처럼 말하지 않는다. 컴파일, 테스트, 에디터 실행, 로그 확인 또는 구체적인 재현 절차 중 적절한 방법으로 실제 동작을 확인한다.
- 검증할 수 없는 환경이면 성공으로 간주하지 않는다. 무엇을 확인했고 무엇을 확인하지 못했는지, 남은 위험과 사용자가 재현할 절차를 명확히 보고한다.
- 테스트 통과나 오류 메시지를 지어내지 않는다. 실행한 명령과 관찰된 결과만 근거로 완료 여부를 판단한다.

## 프로젝트 기준

- 실제 Unity 프로젝트 루트는 `BloodAndBeans/`이다.
- Unity 버전은 `6000.5.9f1`이다. 임의로 업그레이드하거나 다운그레이드하지 않는다.
- 주요 기술은 URP 17.5, Input System 1.20, Netcode for GameObjects 2.13이다.
- 기존 폴더 구조와 주변 코드 스타일을 먼저 따른다. 새 규칙을 적용한다는 이유로 관련 없는 파일을 일괄 정리하지 않는다.
- 게임 규칙의 기준 문서가 있으면 구현보다 먼저 확인한다. 코드와 기획이 다르면 추측으로 한쪽을 바꾸지 말고 차이를 보고한다.

## 작업 순서

1. 코드를 작성·수정하기 전에 `Ponytail` 스킬이 제공되는지 확인하고, 사용할 수 있으면 해당 `SKILL.md`를 끝까지 읽은 뒤 그 절차를 반드시 적용한다.
2. `Ponytail` 스킬이 설치되지 않았거나 접근할 수 없으면 사용한 것처럼 가장하지 않는다. 코드 작성을 시작하기 전에 사용자에게 부재 사실과 필요한 설치 또는 경로 정보를 알린다.
3. 요청 범위와 관련 파일, 호출 경로, 씬·프리팹 연결, 네트워크 권한을 확인한다.
4. 검색, 참조 추적, 로그 또는 재현 결과 없이 원인을 단정하거나 존재하지 않는 API·패키지·오브젝트를 만들어내지 않는다.
5. 문제를 해결하는 가장 작은 변경을 한다. 요청하지 않은 리팩터링과 기능 확장은 하지 않는다.
6. 컴파일, 관련 테스트, 에디터 로그 또는 재현 절차로 검증한다. 실행하지 못한 검증은 실행한 것처럼 말하지 않는다.
7. 완료 시 변경 내용, 검증 결과, 남은 위험만 간결하게 보고한다.

## MCP 연결

- 세션을 시작하면 사용할 수 있는 MCP 서버를 기본으로 연결해 쓴다. 도구가 있는데 손으로 대체하지 않는다.
- `unreal` MCP는 사용하지 않는다. 이 저장소는 Unity 프로젝트다.
- 코드베이스 구조·의존 관계·변경 영향 범위를 파악할 때는 `graphify` MCP를 먼저 쓴다. 결과는 실제 파일로 한 번 더 확인한다.
- 에디터 실행 결과·화면 확인이 필요하면 브라우저 및 데스크톱 제어 MCP를 검증 수단으로 쓴다. 확인하지 못했으면 미검증으로 보고한다.
- MCP가 연결되지 않았거나 호출이 실패하면 사용한 것처럼 말하지 않는다. 부재 사실과 대체로 수행한 확인 방법을 보고한다.

## QA 게이트 — 작업이 끝나면 Codex로 검수한다

**QA는 Codex가 한다.** 구현을 맡은 에이전트가 자기 작업을 검수하지 않는다. 한 단계의 개발이
끝나면, 그리고 기획서·아키텍처 Markdown 작업이 끝나면, 다음 개발을 시작하기 전에 Codex QA를
실행한다.

### 실행 방식

- Codex에서 **QA 서브 에이전트를 여러 개** 띄우고 **역할을 나눠** 맡긴다. 한 에이전트가 전부
  보면 놓치는 것이 생기고, 어디까지 봤는지도 남지 않는다.
- 역할은 최소 다음 넷으로 나눈다. 범위가 늘면 역할을 쪼개되 겹치게 두지 않는다.

| 역할 | 보는 것 |
|---|---|
| **기획 대조** | 기준 기획서와 구현의 차이. 수치·규칙·페이즈 구조·승리 조건이 문서와 같은가 |
| **네트워크 권위** | 서버 권위, RPC 발신자 검증, 팀 격리, 복제 범위, 클라이언트가 조작할 수 있는 값 |
| **코어 루프** | 밤 → 전환 → 낮 → 다음 밤 순환, 밤 재료의 낮 재고 연결, 임대료·페널티·예보 |
| **런타임·회귀** | Unity 실제 플레이, Console 오류, 씬·프리팹 연결 누락, EditMode 테스트, 이전 결함의 재발 |

- **2인 멀티플레이를 반드시 실제로 검증한다.** NGO Host 1 + Client 1(MPPM 가상 플레이어 포함)
  이상으로 붙여 한 판을 돌린다. 팀 격리는 서로 다른 팀 2개로 확인한다 — 한 팀만 띄우면 팀
  간 오염 결함은 드러나지 않는다. 2인에서 확인할 항목은 최소 다음과 같다.
  - 팀 배정이 두 클라이언트에 각각 붙고, 클라이언트마다 자기 팀 값을 읽는가
  - 남의 카페 설비·재료 칸·서빙대·싱크대·완성 게이지를 조작할 수 없는가
  - 안개·박스 개봉·팀 재고·매출·임대료가 팀별로 분리되는가
  - 한쪽의 행동이 다른 쪽의 상태를 바꾸지 않는가
  - Host가 종료했을 때 Client가 안전하게 빠져나오는가

### 보고서

- **기준 기획서를 근거로** 쓴다. "이상해 보인다"가 아니라 "기획서 몇 장 몇 절이 X라고 했는데
  구현은 Y다"로 적는다.
- 결함마다 **어느 부분이 어떻게 잘못됐는지 자세하고 꼼꼼하게** 설명한다. 최소 항목:
  심각도 · 기획서 근거(장·절) · 파일과 라인 · 재현 절차 · 기대 결과 · 실제 결과 · 영향 범위.
- 심각도순으로 정렬한다. **실행하지 못한 항목은 통과가 아니라 `미검증`으로 남긴다.**
- 통과한 항목도 "무엇을 어떻게 확인했는지"를 적는다. 근거 없는 통과는 통과가 아니다.

### 게이트

- 주 에이전트는 QA 보고서를 사용자에게 공유하고, 문서의 잘못된 전제와 완료 표시를 먼저
  고친다.
- **치명·높음 결함과 문서 불일치가 정리되기 전에는 후속 개발을 시작하지 않는다.**
- 개발이 끝나면 같은 시나리오로 Codex QA 회귀 검증을 다시 돌린다. 컴파일 성공이나 EditMode
  테스트 통과만으로 완료를 선언하지 않는다.

## Unity에서 쓰지 말아야 할 방식

### 에셋과 프로젝트 파일

- `Library/`, `Temp/`, `Logs/`, `obj/`, `UserSettings/`와 자동 생성되는 `.csproj`, `.sln`을 직접 수정하거나 커밋하지 않는다.
- Unity 에셋의 `.meta` 파일을 삭제·재생성·누락하지 않는다. 에셋을 파일 시스템에서 이동해야 한다면 대응하는 `.meta`도 함께 이동하고 GUID 보존을 확인한다. 가능하면 Unity Editor에서 이동한다.
- 씬, 프리팹, `ProjectSettings`의 YAML을 대규모 문자열 치환으로 수정하지 않는다. 직렬화 참조가 깨질 수 있는 변경은 에디터에서 수행하고 Missing Script/Reference를 확인한다.
- 패키지, 렌더 파이프라인, 입력 체계, 네트워크 프레임워크를 사용자 승인 없이 추가·교체·업그레이드하지 않는다.
- 서드파티 패키지 원본을 직접 고치지 않는다. 래퍼, 어댑터, 상속 또는 프로젝트 소유 복사본을 우선한다.
- 같은 의미의 에셋을 `_new`, `_final`, `_v2`처럼 복제해 버전을 관리하지 않는다. 버전 이력은 Git을 사용한다.

### MonoBehaviour와 생명주기

- `MonoBehaviour`나 `ScriptableObject`를 `new`로 생성하지 않으며 생성자에 초기화 로직을 넣지 않는다. Unity 생명주기나 팩토리 API를 사용한다.
- 모든 기능을 한 `Manager`, `GameController`, 거대 `MonoBehaviour`에 몰아넣지 않는다. 씬 연결 역할과 순수 게임 규칙을 분리하고 컴포지션을 우선한다.
- `Update`, `FixedUpdate`, `LateUpdate`를 기본 실행 수단으로 쓰지 않는다. 이벤트, 명시적 호출, 코루틴 또는 제한된 주기 갱신으로 충분하면 그것을 사용한다.
- 물리 이동과 `Rigidbody` 처리를 일반 `Update`에서 하지 않는다. 물리 단계가 필요한 처리는 `FixedUpdate`와 Rigidbody API를 사용한다.
- `Awake`, `OnEnable`, `Start`의 호출 순서에 암묵적으로 의존하지 않는다. 필요한 의존성은 직렬화 참조, 명시적 초기화 또는 검증 가능한 조립 지점으로 드러낸다.
- 이벤트를 구독하고 해제하지 않은 채 오브젝트를 파괴하지 않는다. 일반적으로 `OnEnable`/`OnDisable`, 네트워크 객체는 `OnNetworkSpawn`/`OnNetworkDespawn`을 쌍으로 맞춘다.
- 필수 참조 누락을 `?.`나 조용한 조기 반환으로 숨기지 않는다. 선택 참조만 허용하고, 필수 참조는 `OnValidate`, assertion 또는 명확한 오류로 빠르게 실패시킨다.

### 참조와 결합도

- 편의를 위해 전역 mutable static 상태, 새 싱글턴, 서비스 로케이터를 만들지 않는다. 수명과 소유권이 명확한 직접 참조, 인터페이스, 이벤트 또는 조립 루트를 사용한다.
- **탐색과 컴포넌트 조회를 주기 실행 안에 두지 않는다. 이 규칙은 예외 없이 적용한다.**
  - 금지 대상: `GameObject.Find`, `FindObjectOfType`, `FindFirstObjectByType`, `FindAnyObjectByType`, `FindObjectsByType`, `GetComponent` 계열, `GetComponentInChildren`, `GetComponentInParent`, `Camera.main`, `LayerMask.NameToLayer`.
  - 금지 위치: `Update`, `LateUpdate`, `FixedUpdate`, `OnGUI`, 매 틱 코루틴, 주기 갱신 타이머, 그리고 이들이 호출하는 모든 헬퍼. 갱신 주기를 0.1초로 늦춘 것은 예외가 아니다.
  - 대신 `Awake`, `Start`, `OnNetworkSpawn`에서 한 번 찾아 필드에 캐시하거나 `[SerializeField]`로 Inspector에서 연결한다. 레이어·태그는 정수 인덱스로 한 번 풀어 둔다.
  - 참조가 늦게 생기는 경우(로컬 플레이어 스폰 등)에는 `if (cached == null)` 안에서만 한 번 찾고 찾은 뒤에는 다시 찾지 않는다.
  - 스폰·입장·페이즈 전환처럼 **이벤트 한 번**에 도는 탐색은 허용한다. 판단 기준은 호출 횟수가 프레임 수에 비례하는가다.
- 싱글턴을 없앤다는 이유로 모든 통신을 ScriptableObject 이벤트 채널로 바꾸지 않는다. ScriptableObject는 공유 설정·불변 데이터에 우선 사용하며, 런타임 mutable 상태는 플레이 종료와 Domain Reload 설정에도 안전하게 초기화되는 경우에만 사용한다.
- 문자열 기반 `SendMessage`, `BroadcastMessage`, 이름 기반 오브젝트 탐색을 새 코드에 사용하지 않는다.
- 태그, 레이어, 씬 이름, Animator 파라미터, Input Action 이름을 문자열로 하드코딩하지 않는다. 기존 상수·해시 ID·에셋 참조를 재사용하고 없으면 한 곳을 진실의 원천으로 만든다.

### 성능과 비동기 처리

- 프로파일링 근거 없이 복잡한 최적화, 커스텀 풀, Jobs/Burst 전환을 도입하지 않는다. 먼저 Unity Profiler로 병목을 확인한다.
- 핫 패스에서 매 프레임 LINQ, 문자열 포매팅, 배열·리스트 생성, 반복적인 컴포넌트 탐색으로 GC 할당을 만들지 않는다.
- `async void`는 Unity/GUI 이벤트 진입점 외에는 사용하지 않는다. 비동기 작업은 `Task`를 반환하고 오브젝트 수명에 맞는 취소와 예외 처리를 둔다.
- 백그라운드 스레드에서 `GameObject`, `Transform`, `UnityEngine.Object` 등 Unity API를 접근하지 않는다. 결과만 메인 스레드로 전달한다.
- 메인 스레드에서 `.Wait()`, `.Result`, 긴 동기 I/O로 프레임을 막지 않는다.

## Netcode에서 쓰지 말아야 할 방식

- 클라이언트 입력이나 로컬 UI 값을 서버 검증 없이 권위 있는 게임 상태로 확정하지 않는다.
- 서버 전용 상태 변경에는 `IsServer`, 소유자 입력에는 `IsOwner` 등 권한 가드를 빠뜨리지 않는다.
- 위치·게이지·타이머처럼 계속 변하는 값을 매 프레임 RPC로 보내지 않는다. `NetworkVariable`, NetworkTransform, 서버 시간 또는 필요한 빈도의 스냅샷을 사용한다.
- RPC 이름과 방향, 신뢰성, 호출 권한을 모호하게 두지 않는다. NGO 2.13에서 실제 사용 가능한 API를 프로젝트 패키지 기준으로 확인한다.
- 네트워크 콜백 등록을 해제하지 않거나, 스폰 전 `NetworkObject`/`NetworkVariable`이 준비되었다고 가정하지 않는다.
- 서버와 클라이언트가 같은 게임 규칙을 각각 독립 계산해 불일치하게 만들지 않는다. 권위 주체 한 곳에서 결정하고 결과만 동기화한다.

## C# 코드 설계에서 쓰지 말아야 할 방식

- 단일 구현 하나를 위해 의미 없는 인터페이스·추상 클래스·팩토리 계층을 미리 만들지 않는다. 실제 교체 지점이나 테스트 경계가 있을 때만 추상화한다.
- 디자인 패턴을 적용하는 것 자체를 목표로 삼지 않는다. 현재 문제보다 복잡한 DI 컨테이너, 이벤트 버스, 상태 머신, 리포지토리 계층을 도입하지 않는다.
- 상속으로 코드 재사용부터 시도하지 않는다. Unity 컴포넌트 조합과 작은 순수 C# 객체를 우선하고, 명확한 is-a 관계에서만 상속한다.
- 클래스 하나가 입력, UI, 저장, 네트워크, 게임 규칙을 동시에 소유하게 하지 않는다. 변경 이유가 다른 책임은 분리한다.
- public 필드를 Inspector 노출 목적으로 사용하지 않는다. 기본은 `[SerializeField] private`이며 외부 변경이 필요한 경우 의도를 나타내는 메서드나 제한된 프로퍼티를 제공한다.
- 게임 밸런스 수치, 시간, 속도, 가격, 임계값을 메서드 내부에 하드코딩하지 않는다. 의미 있는 상수, 직렬화 설정 또는 설정 에셋으로 이름과 출처를 부여한다.
- 예외를 빈 `catch`로 삼키거나 로그만 남기고 손상된 상태로 계속 진행하지 않는다. 복구하거나 호출자에 전달하거나 명시적으로 실패한다.
- 경고를 없애기 위한 무분별한 null-forgiving, pragma 비활성화, 빈 테스트, 하드코딩된 성공 결과를 사용하지 않는다.
- 읽기 어려운 영리한 한 줄 코드, 과도한 축약어, 타입을 반복하는 헝가리안 표기법을 사용하지 않는다. 프로젝트의 기존 명명과 포맷을 우선한다.
- 주석으로 나쁜 구조를 정당화하지 않는다. 주석은 코드가 무엇을 하는지가 아니라 결정 이유, 네트워크 권한, Unity 제약을 설명한다.
- 주석과 XML 문서 주석(`///`)은 모두 한글로 쓴다. 타입명, API명, 기획서 참조 표기(`doc 5.6.2`)처럼 원문이 식별자인 것만 원문을 유지한다.

## SOLID 적용 원칙

모든 신규 코드와 구조 변경은 SOLID를 기준으로 검토한다. 단, SOLID를 명분으로 현재 요구보다 큰 추상화 계층을 미리 만들지 않는다.

- **SRP(단일 책임):** 클래스는 하나의 변경 이유만 가진다. 입력·표현·네트워크 권위·게임 규칙·저장을 한 클래스에 섞지 않는다. Unity 컴포넌트는 실제 생명주기와 소유권 단위로 나눈다.
- **OCP(개방-폐쇄):** 기존 안정 코드를 반복 수정하지 않고 새 동작을 추가할 수 있게 구성한다. 다만 두 번째 실제 변형이 생기기 전에는 Strategy·Factory·추상 계층을 만들지 않는다.
- **LSP(리스코프 치환):** 상속 타입은 기반 타입의 권한, 상태 전이, 입력·출력 계약을 깨지 않는다. 계약을 지킬 수 없으면 상속하지 않고 컴포지션을 사용한다.
- **ISP(인터페이스 분리):** 소비자가 쓰지 않는 메서드에 의존하지 않도록 역할별로 작고 명확한 인터페이스를 둔다. 구현이 하나뿐이고 교체·테스트 경계가 아니라면 인터페이스를 만들지 않는다.
- **DIP(의존성 역전):** 고수준 게임 규칙은 Unity 오브젝트, UI, Transport와 플랫폼 SDK의 구체 구현에 직접 의존하지 않는다. 순수 규칙·좁은 계약·직렬화 참조를 사용하며, DI 컨테이너나 서비스 로케이터는 도입하지 않는다.
- SOLID 준수 여부는 클래스 수가 아니라 변경 영향 범위, 테스트 가능성, 의존 방향과 계약 보존으로 판단한다.

## 변경 및 검증 가드레일

- 사용자의 기존 변경을 되돌리거나 덮어쓰지 않는다. 관련 없는 파일을 포맷하거나 정리하지 않는다.
- 파괴적 Git 명령, 대량 삭제, 강제 푸시, 브랜치 변경, 커밋은 명시적 요청 없이 하지 않는다.
- 새 기능에는 가능하면 Unity Test Framework의 EditMode 순수 로직 테스트를 우선하고, 생명주기·씬·네트워크 동작은 필요한 범위에서 PlayMode 또는 실제 호스트/클라이언트로 확인한다.
- 테스트가 통과하도록 제품 코드를 하드코딩하거나 실패 테스트를 삭제·완화하지 않는다.
- 컴파일 확인 없이 완료라고 하지 않는다. Unity를 실행할 수 없다면 최소한 수정한 C#의 참조와 API 버전을 실제 프로젝트 파일에서 정적 확인하고, 미검증 상태와 후속 검증 절차를 보고한다.
- 성능 개선을 주장할 때는 변경 전후 프로파일 또는 할당 수치가 있어야 한다.

## 참고한 공개 지침

별 수는 2026-08-24 확인 시점의 대략적인 GitHub 표시이며 품질 보증 수단이 아니다. 규칙은 아래 자료를 그대로 복사하지 않고 이 저장소에 맞게 선별했다.

- [PatrickJS/awesome-cursorrules](https://github.com/PatrickJS/awesome-cursorrules) — 약 40.7k stars. Unity C#을 포함한 에이전트 규칙 모음과 규칙 범위화 방식을 참고했다.
- [justinwasilenko/Unity-Style-Guide](https://github.com/justinwasilenko/Unity-Style-Guide) — 약 635 stars. 기존 프로젝트 스타일 우선, 에셋 구조, 직렬화 필드와 명명 원칙을 참고했다.
- [thomasjacobsen-unity/Unity-Code-Style-Guide](https://github.com/thomasjacobsen-unity/Unity-Code-Style-Guide) — 약 337 stars. Unity 전문가 기반 C# 가독성·일관성 지침을 참고했다.
- [UnityTechnologies/open-project-1](https://github.com/UnityTechnologies/open-project-1) — Unity Technologies 공식 오픈 프로젝트. 싱글턴의 강결합 문제와 이벤트 기반 분리 사례를 참고하되, 모든 통신을 ScriptableObject로 강제하지는 않았다.
- [jbarbier/CLAUDE.md](https://github.com/jbarbier/CLAUDE.md) — 약 483 stars. 검증 가능한 완료 조건과 `CLAUDE.md`/`AGENTS.md` 단일 원본 운영 방식을 참고했다.
- [zeke/agents.md](https://github.com/zeke/agents.md) — 약 152 stars. 추측 억제, 작은 변경, 검증 및 명확한 보고 원칙을 참고했다.
