# Codex QA — 2026-08-24

> 기준 기획서: `Plan_eatyourmeat/1_기획서/BLOOD & BEAN — 게임 기획서.md` v2.6  
> 대상: 아키텍처 1~5단계와 6단계의 이동 권위·팀 복제 차단  
> 방식: 기획 대조 / 네트워크 권위 / 코어 루프 / 런타임·회귀 역할 분리

## 2026-08-24 재검수 결과

- 치명 1건과 높음 12건의 정적 구현 결함을 수정했다. 페이즈 밖 진행 중 제조·게이지도 재검수에서 추가 차단했다. 아래 결함 본문은 최초 발견 근거로 보존한다.
- 함께 해결: Q-S10(수동 배낭 쏟기), Q-N06(예보 RPC 서버 발신 제한), Q-N07(팀 조회 fail-closed).
- 입력·표현 분리 5단계를 완료했다: `BB.Client`, `PlayerInputRouter`, 트리거 후보 기반 `PlayerInteractor`/`IInteractable`, uGUI, `.inputactions` 실제 참조.
- `BB.Game`의 `Keyboard`·`OnGUI`·`Renderer` 참조는 각 0건이며 Input System 어셈블리 참조도 제거했다.
- Unity 6000.5.9f1 최종 컴파일 오류 0건, EditMode **51/51 통과**(job `2ab75a29121b44d1960650521b986489`, 2026-08-24).
- MPPM Host 1 + Client 1에서 확인한 범위는 client0=team0, client1=team1과 양쪽 상대 카페 관찰 객체 0개다. 종료 구간에는 MPPM 패키지의 `Scene file not found: ''` 예외가 발생했다.
- 실제 키 입력 밤→낮 E2E, 설비·박스·안개·재고·매출·임대료의 타팀 비오염, 원격 이동, 2팀 4인, Shutdown→복귀→재접속은 미검증이다. 따라서 최종 QA 게이트는 아직 닫히지 않았다.

## 최초 발견 요약

| 심각도 | 건수 | 게이트 |
|---|---:|---|
| 치명 | 1 | 수정 전 후속 개발 금지 |
| 높음 | 12 | 수정 전 후속 개발 금지 |
| 중간 | 8 | 범위와 승격 조건 기록 |
| 낮음 | 1 | 회귀 방지 시 함께 수정 |

이 표는 최초 발견 시점의 건수다. 현재 게이트 상태는 위 재검수 결과를 따른다. Q-S01은 둘째 날부터 핵심 루프가 진행 불가능해 치명으로, Q-S06은 희귀 보상을 구조적으로 보존하는 경쟁 악용이라 통합 QA에서 높음으로 승격했다.

## 치명·높음 결함

### Q-S01 — 치명 — 둘째 밤부터 숲 박스가 고갈된다

- 기획서 근거: 6.3, 13장. 위치는 고정하고 등급·내용은 매 밤 리롤한다.
- 파일: `Assets/Scripts/Night/ItemBox.cs:55`, `:69`, `:171`
- 재현: 첫 밤에 박스를 비운 뒤 전환→낮→둘째 밤에 같은 박스를 연다.
- 기대: 새 등급·내용을 가진 닫힌 박스.
- 실제: 첫 밤의 등급·열림·빈 슬롯 상태가 유지된다.
- 영향: 다일차 밤→낮 코어 루프가 첫날 뒤 중단된다.

### Q-S02 — 높음 — 확정 예보 주문이 실제 손님에게 적용되지 않는다

- 기획서 근거: 5.5, 5.6. 전환에서 확정한 주문이 다음 낮의 실제 주문이다.
- 파일: `Rules/Forecast.cs:61`, `Economy/TransitionLedger.cs:102`, `Day/CustomerQueue.cs:68`
- 재현: `Forecast.Orders`와 다음 낮 손님의 요구 태그를 비교한다.
- 기대: 70/30 후보와 아메리카노 20% 상한이 실제 손님에게 적용된다.
- 실제: 종족만 전달되고 주문은 `RollOrder`로 다시 무작위 생성된다. `ordersPerDay` 8개를 소진하면 `CustomerQueue.cs:85`가 예보 밖 무작위 손님도 계속 만든다.
- 영향: 밤 재료와 다음 낮 수요의 연결, 주문 상한, 예보된 손님 수가 런타임에서 사라진다.

### Q-S03 — 높음 — 보유 재료 30%가 리젠 풀과 전 팀 합집합을 사용한다

- 기획서 근거: 5.5 규칙 3. 각 팀이 이미 보유한 재료 기준이다.
- 파일: `Rules/Forecast.cs:56`, `Economy/TransitionLedger.cs:152`
- 재현: team0만 Berry를 갖고 Berry가 리젠 풀에 없는 조건으로 양 팀 주문을 만든다.
- 기대: team0의 30% 후보에만 Berry 메뉴가 들어간다.
- 실제: 보유 후보에도 리젠 풀이 섞이고 모든 팀 재고가 합쳐진다.
- 영향: 현재는 Q-S02 때문에 잠복해 있다. 확정 주문을 연결하면 한 팀의 재고가 다른 팀 주문을 오염시킨다.

### Q-S04 — 높음 — 첫 밤과 밤 시작 15초에도 견제가 가능하다

- 기획서 근거: 4장 Night 0, 6.4의 0:00~0:15 보호 구간.
- 파일: `Night/DashHarass.cs:24`, `:42`
- 재현: 첫 밤 시작 직후 상대 근처에서 대시한다.
- 기대: 첫 밤 전체와 이후 밤 첫 15초에는 피해 효과가 없다.
- 실제: 넉백·경직·개봉 중단·재료 낙하가 적용된다.
- 영향: 튜토리얼과 공정한 진입 구간이 성립하지 않는다.

### Q-S05 — 높음 — 커피 머신과 오븐이 레시피 계열을 구분하지 않는다

- 기획서 근거: 5.1, 7.2. 커피는 원두 기반 머신, 디저트는 빵 베이스 기반 오븐이다.
- 파일: `Day/Station.cs:90`, `Day/CoffeeMachine.cs:4`, `Day/Oven.cs:4`
- 재현: 머신에 BreadBase+Berry, 오븐에 Bean+Milk를 넣는다.
- 기대: 잘못된 설비가 재료나 조리 시작을 거부한다.
- 실제: 양쪽 모두 받아 제품으로 만든다.
- 영향: 설비 역할과 디저트 추가 동선이 사라진다.

### Q-S06 — 높음 — 미귀환 손실이 무작위 50%가 아니다

- 기획서 근거: 6.8. 인벤토리 50%가 무작위 소실된다.
- 파일: `Night/PlayerInventory.cs:49`, `:85`
- 재현: Ice와 BloodBean을 함께 들고 반복해서 귀환에 실패한다.
- 기대: 항목 절반이 무작위로 사라진다.
- 실제: 목표 무게에 도달할 때까지 가벼운 재료부터 제거된다.
- 영향: 경량 재료가 희귀 재료의 손실 방패가 된다.

### Q-S07 — 높음 — 검증 빌드에서 제외한 보상이 3등급 슬롯의 45%다

- 기획서 근거: 13장 제외 목록의 블러드 빈·설비 업그레이드.
- 파일: `Night/ItemBox.cs:178`, `Day/IngredientShelf.cs:13`
- 재현: 3등급 박스를 여러 번 열고 낮 선반에서 획득물을 사용한다.
- 기대: 첫 검증 빌드의 네 메뉴에 쓸 수 있는 재료가 나온다.
- 실제: 사용 경로 없는 BloodBean/UpgradePart가 45% 확률로 나온다.
- 영향: 핵심 루프 검증에서 중심부 보상이 죽은 자원이 된다.

### Q-C01 — 높음 — 서버 페이즈 가드가 없어 페이즈 밖 상호작용이 가능하다

- 기획서 근거: 2장, 4장. 밤은 파밍, 낮은 운영이다.
- 파일: `Night/ItemBox.cs:90`, `:128`; `Day/IngredientShelf.cs:56`; `Day/Station.cs:69`; `Day/Sink.cs:59`
- 재현: 낮에 숲 박스를 열거나 밤에 카페 설비를 사용한다.
- 기대: 서버가 현재 페이즈와 다른 요청 및 진행 중 홀드를 거부한다.
- 실제: 거리·팀만 맞으면 처리된다.
- 영향: 제한시간과 자원 경제를 우회할 수 있다.

### Q-N01 — 높음 — 대시·피격 RPC 발신 권한이 열려 있다

- 기획서/아키텍처 근거: 6.6, D1/D2. Host가 피해 결과를 단독 결정한다.
- 파일: `Night/DashHarass.cs:42`, `:68`; NGO 2.13.1 `RpcAttributes.cs:24`, `:91`
- 재현: 변조 클라이언트가 타인 객체의 `DashRpc` 또는 임의 값의 `HitRpc`를 호출한다.
- 기대: 대시는 Owner만 요청하고 피격은 Server만 발신한다.
- 실제: unified RPC 기본 `InvokePermission`은 Everyone이다.
- 영향: 타인 쿨다운·경직·낙하·홀드 진행을 임의 조작할 수 있다.

### Q-N02 — 높음 — Owner 권위 위치를 서버 검증의 진실로 사용한다

- 기획서/아키텍처 근거: 6.4~6.8, D9, §2.4 이동 서버 권위.
- 파일: `Player/PlayerMove.cs:15`, `Player.prefab:175`의 `AuthorityMode: 1`; NGO `NetworkTransform.cs:1301`에서 1은 Owner 권위. 거리 소비처 `Station.cs:142`, `ItemBox.cs:210`, `ReturnZone.cs:52`
- 재현: 변조 클라이언트가 자기 위치를 박스·귀환점·피해자 근처로 순간 이동한다.
- 기대: 서버가 입력으로 이동을 계산하고 권위 위치를 소유한다.
- 실제: Owner가 직접 transform을 변경하고 Owner 권위 NetworkTransform이 복제한다.
- 영향: 이동 시간·안개·귀환·거리·견제 규칙을 우회한다.

### Q-N03 — 높음 — 상대 팀 비공개 상태가 모든 클라이언트에 복제된다

- 기획서 근거: 3.1. 매출만 공개하고 재료·설비·캐릭터는 비공개다.
- 파일: `TeamStock.cs:13`, `PlayerInventory.cs:16`, `Station.cs:14`, `Dish.cs:13`, `Customer.cs:10`, `FogOfWar.cs:24`
- 재현: 클라이언트에서 상대 카페와 플레이어의 NetworkList/NetworkVariable을 읽는다.
- 기대: 해당 팀과 서버에만 복제되거나 상대 객체 자체가 관찰 범위 밖이다.
- 실제: 기본 read permission Everyone으로 원본 상태가 전달된다.
- 영향: 상대 재고·제조·그릇·주문·안개 경로를 볼 수 있다.

### Q-N03B — 높음 — 박스 개봉·공개 상태가 전 팀 공용이다

- 기획서 근거: v2.6 6.5.3. 공개 상태는 같은 팀 안에서만 공유한다.
- 파일: `Night/ItemBox.cs:23`, `:24`, `:97`, `:192`, `:195`
- 재현: team0이 박스를 열고 가려진 슬롯 공개를 기다린 뒤 team1이 같은 박스에 접근한다.
- 기대: team1은 자기 팀 기준으로 다시 개봉하고 가려진 정보를 기다린다.
- 실제: 박스당 단일 `opened`·`revealAt`을 사용해 team0의 상태를 그대로 공유하고, `slots`도 Everyone-read라 열기 전부터 내용 원본이 모든 팀에 복제된다.
- 영향: 단순 복제 누출을 넘어 서버 상태 모델 자체가 팀 공개 규칙을 표현하지 못한다.

### Q-N04 — 높음 — 대시가 같은 팀원을 공격한다

- 근거/파일: 2.1·6.6의 상대 견제, `DashHarass.cs:77`.
- 재현: 같은 팀원과 상대가 있는 상태에서 팀원을 가장 가까이 두고 대시한다.
- 기대/실제: 가장 가까운 상대만 맞아야 하나 팀 구분 없이 가장 가까운 플레이어가 맞는다.
- 영향: 2인 협동 팀원이 서로의 홀드·적재·이동을 파괴한다.

## 해결된 문서 결함

### Q-D01 — 해결됨 — 기준 기획서가 확정 방향과 충돌했다

- 근거: 기존 v2.5의 6.1/6.5.3/12장과 인수인계 §8 M1~M3.
- 실제: 전원 안개 공유·전원 박스 공개·2D 도트와 팀 공유·3D 구현이 충돌했다.
- 조치: 본 QA에서 기준 기획서를 v2.6으로 올리고 팀 내 공유와 3D 탑다운으로 수정했다.
- 남은 코드 결함은 Q-N03과 Q-N03B로 분리했다.

## 중간·낮음 결함

### Q-S08 — 중간 — Perfect가 실제 수령자가 아닌 줄 맨 앞 손님을 회복한다

- 근거/파일: 5.2·5.5, `Station.cs:154`.
- 재현: 두 번째 손님용 제품을 Perfect로 만들고 두 손님의 인내심을 비교한다.
- 기대/실제: 제품을 실제로 받은 손님이 회복해야 하나 제작 순간의 첫 손님이 회복한다.
- 영향: 보상이 잘못된 대상과 시점에 적용된다.

### Q-S09 — 중간 — 박스에서 원하는 슬롯을 고를 수 없다

- 근거/파일: 6.5.1, `ItemBox.cs:133-135`.
- 재현: 서로 다른 공개 재료 중 두 번째 슬롯을 먼저 선택한다.
- 기대/실제: 선택 슬롯을 담아야 하나 첫 공개 슬롯부터 자동 획득한다.
- 영향: 무게를 고려한 파밍 선택이 사라진다.

### Q-S10 — 해결됨 — 수동 배낭 쏟기가 없다

- 근거/파일: 6.7·11장, `PlayerInventory.cs`, `DashHarass.cs:54`.
- 재현: 밤에 재료를 든 채 별도 쏟기 입력을 시도한다.
- 기대/실제: 전부 임시 더미로 쏟아야 하나 피격 낙하 외 호출 경로가 없다.
- 영향: 과적 해소와 전략적 자원 양도가 불가능하다.

### Q-S11 — 중간 — 마지막 날 임대료와 최종 승자 결산이 없다

- 근거/파일: 3.1·3.2, `GamePhase.cs:60-65`, `TransitionLedger.cs:60`, `PhaseHud.cs:52`.
- 재현: 마지막 낮 타이머 종료 후 Rent 상태와 화면을 확인한다.
- 기대/실제: 마지막 임대료를 정산하고 1위 팀 승리를 선언해야 하나 `Finished`만 세우고 일반 랭킹을 유지한다.
- 영향: 마지막 임대료와 명시적 경기 종료 결과가 누락된다.

### Q-C02 — 중간 — 전환 화면에 매출·임대료 결과·순위가 없다

- 근거/파일: 4장, `PhaseHud.cs:67`.
- 재현: 낮 종료 뒤 전환 HUD를 본다.
- 기대/실제: 매출·임대료·순위·예보가 보여야 하나 종족 수와 인기 재료만 보인다.
- 영향: 미납과 페널티의 원인을 플레이어가 알 수 없다.

### Q-N05 — 중간 — 연결 승인과 참가 명단 검증이 없다

- 근거/파일: 10장, `SampleScene.unity:11481`, `MatchDirector.cs:82`.
- 재현: 세션 주소를 아는 추가 클라이언트를 연결한다.
- 기대/실제: 정한 명단·인원만 승인해야 하나 자동 스폰 후 라운드로빈 배정한다.
- 영향: 초과·비인가 참가자가 팀 상태와 결과에 개입한다.

### Q-N06 — 해결됨 — 변조 클라이언트가 표시용 예보 RPC를 위조할 수 있다

- 근거/파일: 서버 예보 권위, `TransitionLedger.cs:131`, NGO `RpcAttributes.cs:91`의 기본 Everyone.
- 재현: 변조 빌드에서 임의 raceCounts/popular로 `ForecastRpc`를 호출한다.
- 기대/실제: Server만 발신해야 하나 모든 연결 Client가 호출 권한을 가진다.
- 영향: 실제 가격 원본은 못 바꿔도 전원의 파밍 전략 UI를 속인다.

### Q-N07 — 해결됨 — 팀 조회 실패가 team0 권한으로 열린다

- 근거/파일: `PlayerTeam.cs:34`, `Cafe.cs:43`.
- 재현: PlayerTeam 누락·초기화 실패 상태로 team0 설비 RPC를 보낸다.
- 기대/실제: -1로 거부해야 하나 0을 반환해 team0 비교를 통과한다.
- 영향: 프리팹 회귀가 team0 설비 권한 상승으로 이어진다.

### Q-C03 — 중간 — 밤 1분 30초 경보와 귀환 방향 표시가 없다

- 근거/파일: 6.4, 시간 원천 `GamePhase.cs:23-25`, 밤 HUD 분기 `PhaseHud.cs:61`에 경보·귀환 방향 처리 없음.
- 재현: 밤 경과 90초에 오디오·HUD·월드 표시를 확인한다.
- 기대/실제: 종소리와 각 플레이어의 귀환 방향이 보여야 하나 아무 이벤트나 표시가 없다.
- 영향: 과적 귀환 판단에 필요한 경고가 사라진다.

## 정적 통과

- `GamePhase.NextPhase`: Night→Transition→Day→Night 반복 순서.
- 씬의 120/10/120초·3일: 첫 검증 빌드 조건.
- `Rent`: 임대료 표와 무이자 부채, 연속 미납 페널티.
- `ReturnZone`→`TeamStock`→`IngredientShelf`: 미귀환 손실 뒤 잔량 적립과 비상비 재료 차감 경로.
- `HoldTimer`: 서버 시간 기반 개봉·담기·세척.
- 낮 설비 RPC: 발신자 ID 기반 거리·팀 검증.
- `CompletionGauge`: 발신자 팀 검증.
- `SalePrice`·`Scoreboard`: 판매가 배수와 누적 매출 순위.
- EditMode: 51/51 통과(job `2ab75a29121b44d1960650521b986489`, 2026-08-24). 결과 파일은 워크스페이스에 복사하지 않았으며 Unity 기본 경로 `C:/Users/hope/AppData/LocalLow/DefaultCompany/BloodAndBeans/TestResults.xml`에 있다.
- 컴파일: 같은 테스트 실행 뒤 Unity Console C# 컴파일 오류 0건.

## 미검증

- 기획 빌드 조건인 2팀 4인의 협동 한 판.
- 밤 재료→낮 한 잔 판매→임대료→다음 밤 페널티의 실제 조작 E2E.
- 런타임의 Shutdown 버튼으로 Host가 나간 뒤 Client가 같은 연결 화면에서 재접속하는 세션 수명. MPPM Play 종료 시 동반 종료는 확인했으나 `Scene file not found: ''` 예외가 발생했다.
- 동심원 숲·카페 동선·박스 외형/발광·미니맵·최종 결과 화면의 실제 플레이 품질.

실행하지 않은 항목은 통과로 계산하지 않는다.
